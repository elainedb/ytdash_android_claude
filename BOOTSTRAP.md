Fresh native-Android workspace. applicationId = com.example.ytdash.
Use the official Android skills in skills/ to scaffold the Gradle/Compose project
(JDK 17, AGP 8.7+, Kotlin 2.x, compileSdk 35). Do not copy code from anywhere else.
